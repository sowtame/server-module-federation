{
  "name": "dynamic-node",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "host",
    "remote"
  ],
  "scripts": {
    "start": "lerna run --scope=dynamic-node_* --parallel start",
    "build": "lerna run --scope=dynamic-node_* build",
    "serve": "lerna run --scope=dynamic-node_* --parallel serve",
    "clean": "lerna run --scope=dynamic-node_* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
